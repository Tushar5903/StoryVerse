# StoryVerse pages

The route implementations are being split from the original single-file prototype into page folders. Shared API contracts live in `src/services`, motion presets in `src/animations`, and shared UI primitives in `src/components`.
